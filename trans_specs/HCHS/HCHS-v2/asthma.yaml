- class_derivations:
    Condition:
      populated_from: pht004715
      slot_derivations:
        associated_participant:
          populated_from: phv00226250
        associated_visit:
          value: HCHS EXAM
          range: string
        condition_concept:
          value: MONDO:0004979
          range: string
        condition_status:
          populated_from: phv00226252
          value_mappings:
            '0': ABSENT
            '1': PRESENT
            '9': UNKNOWN
        condition_provenance:
          value: PATIENT_SELF-REPORTED_CONDITION
          range: string
        relationship_to_participant:
          value: ONESELF
          range: string
- class_derivations:
    Condition:
      populated_from: pht004715
      slot_derivations:
        associated_participant:
          populated_from: phv00226250
        associated_visit:
          value: HCHS EXAM
          range: string
        age_at_condition_start:
          expr: case(({phv00226387} == 1, {phv00258081} * 365))
        age_at_condition_end:
          expr: case(({phv00226387} == 1, case(({phv00526878} == 0, {phv00526878} * 365))))
        condition_concept:
          value: MONDO:0004979
          range: string
        condition_status:
          expr: case(({phv00226387} == 0, 'ABSENT'), ({phv00526887} == 1 and {phv00526889} == 1, 'PRESENT'), ({phv00526887} == 1 and {phv00526889} == 0, 'HISTORICAL'), (True, 'UNKNOWN'))
        condition_provenance:
          populated_from: phv00226388
          value_mappings:
            '0': PATIENT_SELF-REPORTED_CONDITION
            '1': CLINICAL_DIAGNOSIS
            '9': UNKNOWN
        relationship_to_participant:
          value: ONESELF
          range: string
- class_derivations:
    Condition:
      populated_from: pht004715
      slot_derivations:
        associated_participant:
          populated_from: phv00226250
        associated_visit:
          value: HCHS EXAM
          range: string
        age_at_condition_start:
          expr: case(({phv00226387} == 1, {phv00258081} * 365))
        age_at_condition_end:
          expr: case(({phv00226387} == 1, case(({phv00526878} == 0, {phv00526878} * 365))))
        condition_concept:
          value: MONDO:0004979
          range: string
        condition_status:
          expr: "case(({phv00526179} == 0, 'ABSENT'), ({phv00526180} == 0, 'HISTORICAL'),
            (True, 'PRESENT'))"
        condition_provenance:
          expr: case(({phv00526179} == 1 and {phv00526181} == 1, 'CLINICAL_DIAGNOSIS'), (True, 'PATIENT_SELF-REPORTED_CONDITION'))
        relationship_to_participant:
          value: ONESELF
          range: string
